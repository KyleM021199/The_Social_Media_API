const { ObjectId } = require('mongoose').Types;
const {User, Thought} = require('../models');

module.exports = {
    //for /thoughts route
    //Get all thoughts
    
    //Get single thought by _id

    // Post a new thought


    // Update a thought

    // Delete a thought 

}